<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="EventsPbMobile.Controls.ActivityBtn">
  <ContentView.Content>
        <Grid Margin="14,8,14,8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Button BackgroundColor="Transparent" Grid.RowSpan="2" Clicked="Button_OnClicked"/>
            <Label Text="{Binding Title}" FontSize="20"  Grid.Row="0" TextColor="#676767"
                               LineBreakMode="WordWrap" Opacity="87" FontAttributes="Bold"/>
            <Label Text="{Binding PlaceAndDate}" Grid.Row="1" FontSize="16" TextColor="#676767" Opacity="87" />
        </Grid>
    </ContentView.Content>
</ContentView>

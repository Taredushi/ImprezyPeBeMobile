<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="EventsPbMobile.Pages.ReminderNotifySelect">
  <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
  <ListView x:Name="TimeSelection" ItemSelected="DisableSelection">
    <ListView.ItemTemplate>
      <DataTemplate>
        <ViewCell Height="50">
          <ViewCell.View>
            <Grid>
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
              </Grid.ColumnDefinitions>
              <Grid.RowDefinitions>
                <RowDefinition Height="25" />
              </Grid.RowDefinitions>

              <Label Text="{Binding ReminderTime}" Grid.Row="0" Grid.Column="0" FontSize="20"/>
              <Switch IsToggled="{Binding Selected}" Grid.Row="0" Grid.Column="1"></Switch>

            </Grid>
          </ViewCell.View>
        </ViewCell>
      </DataTemplate>
    </ListView.ItemTemplate>
  </ListView>
</ContentPage>